#include <stdio.h>
int main()
{
    float a , b ;
    printf("this program is used to calculate the area of a rectangle\n");
    printf("type the length of the rectangle\n");
    scanf("%f",&a);
    printf("type the breadth of the rectangle\n");
    scanf("%f",&b);
    printf("the area of rectangle is %f ",a*b);

}