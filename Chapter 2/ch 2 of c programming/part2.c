#include<stdio.h>
#include<math.h>
int main() 

    {
    // float a = 1.1;
    // float b = a + 8.9;
    // printf("the value of b is %f\n", b);
    // printf("the value of b is %f\n", 5.1+2.9);
    // printf("the value of b is %f\n", pow(a, b) );
    
    printf("the value of k is %f", 3.0/9);
    return 0;
}